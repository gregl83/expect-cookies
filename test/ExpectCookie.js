var should = require('should');

var Cookie = require('../');

describe('Cookie', function() {
  it('has been set', function(done) {
    done();
  });

  it('has been set for the first time', function(done) {
    done();
  });

  it('has been reset', function(done) {
    done();
  });

  it('has been renewed', function(done) {
    done();
  });

  it('is NOT set', function(done) {
    done();
  });
});